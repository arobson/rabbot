sudo: required
services:
  - docker
os:
  - linux

language: node_js
node_js:
  - "22"
  - "node"

before_script:
  - npm run bootstrap-container
  - sleep 10

script:
  - c8 npm test && c8 report --reporter=text-lcov | coveralls

after_script:
  - npm run remove-container
